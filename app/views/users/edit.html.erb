<% provide(:title,"Редактирование пользователя «#{@user.name}»") %>

<h2 class='page_title'><%= yield(:title) %></h2>

<div class='edit col-md-4 col-md-offset-4'>
	<%= form_for(@user) do |f| %>
		<%= render 'shared/error_messages.html.erb', object:f.object %>
		<%= render 'form', f:f, mode:'edit' %>
	<% end %>
</div>