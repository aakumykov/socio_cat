<% provide(:title,'Восстановление пароля') %>

<h1 class='page_title'><%= yield(:title) %></h1>

<%= form_tag(reset_password_path) do %>
	<%= label_tag :email, 'Электронная почта' %>
	<%= text_field_tag(:email) %>
	<%= submit_tag 'Отправить' %>
<% end %>
