<% provide(:title,'Новый объект') %>

<h2 class='page_title'><%= yield(:title) %></h2>

<div class='col-md-6 col-md-offset-3'>
	<%= render 'form', cancel_path:matters_path, button_text:'Создать', matter:@obj %>
</div>