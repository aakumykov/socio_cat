<% provide(:title,'Карточки') %>

<h1 class='page_title'><%= yield(:title) %></h1>


<div class='span8 offset2'>
	<%= render 'card_button_new' %>

	<% @all_cards.each do |card| %>
	<%= render 'card_display', card: card, editable: false %>
	<% end %>
</div>