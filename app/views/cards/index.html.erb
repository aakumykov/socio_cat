<% provide(:title,'Карточки') %>

<h1 class='page_title'><%= yield(:title) %></h1>


<div class='span8 offset2'>
	<%= render 'comfort_buttons' %>

	<% @list.each do |card| %>
	<%= render 'display', card: card, editable: false %>
	<% end %>
</div>