<div class='area audio_content'>
	<div class='caption'>Звук</div>
	<% if value.nil? %>
		<%= render 'cards/content/display/none' %>
	<% else %>
		<%= audio_tag value.url, controls:true %>
	<% end %>
</div>